import { defineStore } from "pinia"

export const useAuthStore = defineStore({
    id: 'auth',
    state: () => ({

    }),
    getters: {
        // something
    },
    actions: {
        // something
    }
})